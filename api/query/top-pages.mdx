---
title: "Get Top Pages"
description: "Retrieve the top pages by visits and unique users over a selected date range."
openapi: "GET /v0/top_pages"
---

Returns the most-visited pages, ranked by visits, with unique visitor counts. Use `limit` and `offset` for pagination.
