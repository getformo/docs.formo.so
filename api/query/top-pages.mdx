---
title: "Get Top Pages"
description: "Top pages by visits and users over the selected window."
openapi: "GET /v0/top_pages"
---

Returns the most-visited pages, ranked by visits, with unique visitor counts. Use `limit` and `skip` for pagination.
