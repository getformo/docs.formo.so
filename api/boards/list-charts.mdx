---
title: 'List Charts'
description: 'Retrieve the charts attached to a dashboard board. Returns lightweight chart summaries by default; pass include=results to execute each chart query and receive full charts with results.'
openapi: 'GET /v0/boards/{boardId}/charts'
---
