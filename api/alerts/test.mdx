---
title: 'Test Alert'
description: 'Send a test delivery for an alert to its webhook recipients (or to recipient overrides). Makes real signed HTTP requests to the configured URLs and returns per-URL delivery results.'
openapi: 'POST /v0/alerts/{alertId}/test'
---
