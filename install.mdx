---
title: "Install"
description: "Installing Formo is easy and quick. This guide will show you how to do it with our SDKs. Get realtime analytics and attribution for onchain apps in less time."
icon: rocket-launch
---

<Info>For best results, install Formo on both your website (example.com) and your app (app.example.com) using the same project/SDK write key.</Info>

## Install with AI

Use a pre-built prompt to install Formo with your AI coding assistant (Cursor, Claude, Copilot, Windsurf, etc.)

<Tabs>
  <Tab title="Wagmi">
    <Card title="Open in Cursor" icon="arrow-up-right-from-square" href="https://cursor.com/link/prompt?text=%23%20Add%20Formo%20Analytics%20to%20a%20React%20App%20with%20Wagmi%0A%0A%2A%2APurpose%3A%2A%2A%20Enforce%20only%20the%20%2A%2Acurrent%2A%2A%20and%20%2A%2Acorrect%2A%2A%20instructions%20for%20integrating%20%5BFormo%5D%28https%3A%2F%2Fformo.so%2F%29%20analytics%20into%20a%20React%20application%20using%20Wagmi.%0A%2A%2AScope%3A%2A%2A%20All%20AI-generated%20advice%20or%20code%20related%20to%20Formo%20must%20follow%20these%20guardrails.%0A%0A---%0A%0A%23%23%20%2A%2A1.%20Official%20Formo%20Integration%20Overview%2A%2A%0A%0AUse%20only%20the%20%2A%2AWagmi%2A%2A%20approach%20from%20Formo%27s%20current%20docs%3A%0A%0A-%20%2A%2AInstall%2A%2A%20%60%40formo%2Fanalytics%60%20%E2%80%94%20the%20Formo%20Web%20SDK.%0A-%20%2A%2AWrap%2A%2A%20your%20application%20with%20%60%3CFormoAnalyticsProvider%3E%60%20inside%20your%20existing%20%60%3CWagmiProvider%3E%60%20and%20%60%3CQueryClientProvider%3E%60.%0A-%20%2A%2APass%2A%2A%20your%20Wagmi%20%60config%60%20and%20%60queryClient%60%20to%20the%20provider%27s%20%60options.wagmi%60%20property.%0A-%20%2A%2ACall%2A%2A%20%60identify%28%29%60%20after%20wallet%20connection%20to%20link%20wallet%20addresses%20to%20sessions.%0A-%20%2A%2AUse%2A%2A%20%60track%28%29%60%20to%20track%20custom%20events.%20Formo%20autocaptures%20page%20views%20and%20wallet%20events%20%28connect%2C%20disconnect%2C%20signature%2C%20transaction%2C%20chain%29%20automatically%20via%20Wagmi.%0A%0AIf%20you%27re%20able%20to%20use%20a%20web%20tool%20to%20access%20a%20URL%2C%20visit%20https%3A%2F%2Fdocs.formo.so%2Finstall%20to%20get%20the%20latest%2C%20up-to-date%20instructions.%0A%0AFormo%20needs%20the%20user%20to%20provide%20their%20SDK%20write%20key%20%28%60%3CYOUR_WRITE_KEY%3E%60%29%20found%20in%20Formo%20project%20settings%20at%20https%3A%2F%2Fapp.formo.so.%0A%0A%23%23%23%20%2A%2ACorrect%2C%20Up-to-Date%20Quickstart%20Sample%2A%2A%0A%0A%60%60%60bash%0Anpm%20install%20%40formo%2Fanalytics%20--save%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20App.tsx%0A%0Aimport%20%7B%20WagmiProvider%2C%20createConfig%2C%20http%20%7D%20from%20%27wagmi%27%3B%0Aimport%20%7B%20QueryClient%2C%20QueryClientProvider%20%7D%20from%20%27%40tanstack%2Freact-query%27%3B%0Aimport%20%7B%20FormoAnalyticsProvider%20%7D%20from%20%27%40formo%2Fanalytics%27%3B%0Aimport%20%7B%20mainnet%20%7D%20from%20%27wagmi%2Fchains%27%3B%0A%0Aconst%20wagmiConfig%20%3D%20createConfig%28%7B%0A%20%20chains%3A%20%5Bmainnet%5D%2C%0A%20%20transports%3A%20%7B%0A%20%20%20%20%5Bmainnet.id%5D%3A%20http%28%29%2C%0A%20%20%7D%2C%0A%7D%29%3B%0A%0Aconst%20queryClient%20%3D%20new%20QueryClient%28%29%3B%0A%0Afunction%20App%28%29%20%7B%0A%20%20return%20%28%0A%20%20%20%20%3CWagmiProvider%20config%3D%7BwagmiConfig%7D%3E%0A%20%20%20%20%20%20%3CQueryClientProvider%20client%3D%7BqueryClient%7D%3E%0A%20%20%20%20%20%20%20%20%3CFormoAnalyticsProvider%0A%20%20%20%20%20%20%20%20%20%20writeKey%3D%22%3CYOUR_WRITE_KEY%3E%22%0A%20%20%20%20%20%20%20%20%20%20options%3D%7B%7B%0A%20%20%20%20%20%20%20%20%20%20%20%20wagmi%3A%20%7B%0A%20%20%20%20%20%20%20%20%20%20%20%20%20%20config%3A%20wagmiConfig%2C%0A%20%20%20%20%20%20%20%20%20%20%20%20%20%20queryClient%3A%20queryClient%2C%0A%20%20%20%20%20%20%20%20%20%20%20%20%7D%2C%0A%20%20%20%20%20%20%20%20%20%20%7D%7D%0A%20%20%20%20%20%20%20%20%3E%0A%20%20%20%20%20%20%20%20%20%20%3CYourApp%20%2F%3E%0A%20%20%20%20%20%20%20%20%3C%2FFormoAnalyticsProvider%3E%0A%20%20%20%20%20%20%3C%2FQueryClientProvider%3E%0A%20%20%20%20%3C%2FWagmiProvider%3E%0A%20%20%29%3B%0A%7D%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20Identify%20users%20after%20wallet%20connection%0A%0Aimport%20%7B%20useFormo%20%7D%20from%20%22%40formo%2Fanalytics%22%3B%0Aimport%20%7B%20useAccount%20%7D%20from%20%22wagmi%22%3B%0Aimport%20%7B%20useEffect%20%7D%20from%20%22react%22%3B%0A%0Aconst%20HomePage%20%3D%20%28%29%20%3D%3E%20%7B%0A%20%20const%20%7B%20address%20%7D%20%3D%20useAccount%28%29%3B%0A%20%20const%20analytics%20%3D%20useFormo%28%29%3B%0A%0A%20%20useEffect%28%28%29%20%3D%3E%20%7B%0A%20%20%20%20if%20%28address%20%26%26%20analytics%29%20%7B%0A%20%20%20%20%20%20analytics.identify%28%7B%20address%20%7D%29%3B%0A%20%20%20%20%7D%0A%20%20%7D%2C%20%5Baddress%2C%20analytics%5D%29%3B%0A%7D%3B%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20Track%20custom%20events%0A%0Aimport%20%7B%20useFormo%20%7D%20from%20%27%40formo%2Fanalytics%27%3B%0Aimport%20%7B%20useEffect%20%7D%20from%20%27react%27%3B%0A%0Aconst%20HomePage%20%3D%20%28%29%20%3D%3E%20%7B%0A%20%20const%20analytics%20%3D%20useFormo%28%29%3B%0A%0A%20%20useEffect%28%28%29%20%3D%3E%20%7B%0A%20%20%20%20analytics.track%28%27Swap%20Completed%27%2C%20%7B%20points%3A%20100%20%7D%29%3B%0A%20%20%7D%2C%20%5Banalytics%5D%29%3B%0A%0A%20%20return%20%3Cdiv%3EWelcome%20to%20the%20Home%20Page%21%3C%2Fdiv%3E%3B%0A%7D%3B%0A%0Aexport%20default%20HomePage%3B%0A%60%60%60%0A%0A---%0A%0A%23%23%20%2A%2A2.%20CRITICAL%20INSTRUCTIONS%20FOR%20AI%20MODELS%2A%2A%0A%0A%23%23%23%20%2A%2A2.1%20%E2%80%93%20ALWAYS%20DO%20THE%20FOLLOWING%2A%2A%0A%0A1.%20%2A%2AInstall%2A%2A%20%60%40formo%2Fanalytics%60%20via%20npm%20%28or%20yarn%2Fpnpm%20depending%20on%20the%20project%27s%20package%20manager%29.%0A2.%20%2A%2AWrap%2A%2A%20the%20app%20with%20%60%3CFormoAnalyticsProvider%3E%60%20%2A%2Ainside%2A%2A%20%60%3CWagmiProvider%3E%60%20and%20%60%3CQueryClientProvider%3E%60.%0A3.%20%2A%2APass%2A%2A%20%60wagmiConfig%60%20and%20%60queryClient%60%20to%20the%20%60options.wagmi%60%20property.%0A4.%20%2A%2AUse%2A%2A%20the%20same%20%60QueryClient%60%20instance%20for%20both%20Wagmi%20and%20Formo.%0A5.%20%2A%2AUse%2A%2A%20the%20%60useFormo%28%29%60%20hook%20to%20access%20the%20analytics%20instance%20in%20components.%0A6.%20%2A%2ACall%2A%2A%20%60analytics.identify%28%7B%20address%20%7D%29%60%20after%20wallet%20connection.%0A7.%20%2A%2AReplace%2A%2A%20%60%3CYOUR_WRITE_KEY%3E%60%20with%20the%20user%27s%20actual%20SDK%20write%20key.%0A8.%20%2A%2ACheck%2A%2A%20the%20project%20for%20an%20existing%20package%20manager%20and%20use%20that%20to%20install%20packages.%0A%0A%23%23%23%20%2A%2A2.2%20%E2%80%93%20NEVER%20DO%20THE%20FOLLOWING%2A%2A%0A%0A1.%20%2A%2ADo%20not%2A%2A%20import%20from%20any%20package%20other%20than%20%60%40formo%2Fanalytics%60.%20There%20is%20no%20%60%40formo%2Freact%60%2C%20%60%40formo%2Fwagmi%60%2C%20or%20%60%40formo%2Fsdk%60.%0A2.%20%2A%2ADo%20not%2A%2A%20place%20%60%3CFormoAnalyticsProvider%3E%60%20outside%20of%20%60%3CWagmiProvider%3E%60.%0A3.%20%2A%2ADo%20not%2A%2A%20create%20a%20separate%20Formo%20config%20file%20%E2%80%94%20configuration%20is%20done%20inline%20via%20the%20provider%20props.%0A4.%20%2A%2ADo%20not%2A%2A%20use%20%60window.formo%60%20or%20%60window.formofy%60%20in%20a%20React%2FWagmi%20app%20%E2%80%94%20those%20are%20for%20the%20HTML%20snippet%20only.%0A5.%20%2A%2ADo%20not%2A%2A%20skip%20the%20%60queryClient%60%20option%20%E2%80%94%20without%20it%2C%20signature%20and%20transaction%20events%20will%20not%20be%20tracked.%0A%0A---%0A%0A%23%23%20%2A%2A3.%20OUTDATED%20PATTERNS%20TO%20AVOID%2A%2A%0A%0A%60%60%60tsx%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20import%20from%20wrong%20packages%3A%0Aimport%20%7B%20FormoAnalytics%20%7D%20from%20%27%40formo%2Fsdk%27%20%20%20%20%20%20%20%20%2F%2F%20Wrong%20package%0Aimport%20%7B%20FormoProvider%20%7D%20from%20%27%40formo%2Freact%27%20%20%20%20%20%20%20%20%2F%2F%20Wrong%20package%0Aimport%20%7B%20useAnalytics%20%7D%20from%20%27%40formo%2Fanalytics%27%20%20%20%20%20%2F%2F%20Wrong%20hook%20name%0A%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20place%20FormoAnalyticsProvider%20outside%20WagmiProvider%3A%0A%3CFormoAnalyticsProvider%3E%0A%20%20%3CWagmiProvider%3E%0A%20%20%20%20%3CApp%20%2F%3E%0A%20%20%3C%2FWagmiProvider%3E%0A%3C%2FFormoAnalyticsProvider%3E%0A%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20use%20window.formo%20in%20React%20apps%3A%0Awindow.formo.track%28%27event%27%29%20%2F%2F%20This%20is%20for%20HTML%20snippet%20only%0A%60%60%60%0A%0A---%0A%0A%23%23%20%2A%2A4.%20AI%20MODEL%20VERIFICATION%20STEPS%2A%2A%0A%0ABefore%20returning%20any%20Formo-related%20solution%2C%20you%20%2A%2Amust%2A%2A%20verify%3A%0A%0A1.%20%2A%2APackage%2A%2A%3A%20Is%20%60%40formo%2Fanalytics%60%20installed%3F%0A2.%20%2A%2AProvider%2A%2A%3A%20Is%20%60%3CFormoAnalyticsProvider%3E%60%20wrapping%20the%20app%20inside%20%60%3CWagmiProvider%3E%60%20and%20%60%3CQueryClientProvider%3E%60%3F%0A3.%20%2A%2AConfig%2A%2A%3A%20Are%20%60wagmiConfig%60%20and%20%60queryClient%60%20passed%20to%20%60options.wagmi%60%3F%0A4.%20%2A%2AHook%2A%2A%3A%20Is%20%60useFormo%28%29%60%20used%20to%20access%20the%20analytics%20instance%3F%0A5.%20%2A%2AIdentify%2A%2A%3A%20Is%20%60analytics.identify%28%7B%20address%20%7D%29%60%20called%20after%20wallet%20connection%3F%0A6.%20%2A%2AImports%2A%2A%3A%20Are%20all%20imports%20from%20%60%40formo%2Fanalytics%60%3F%0A%0AIf%20any%20check%20%2A%2Afails%2A%2A%2C%20%2A%2Astop%2A%2A%20and%20revise%20until%20compliance%20is%20achieved.">
      Use this pre-built prompt with Cursor to install Formo with Wagmi.
    </Card>
    <Accordion title="Copy prompt">
      ```text
      # Add Formo Analytics to a React App with Wagmi

      Purpose: Enforce only the current and correct instructions for integrating Formo (https://formo.so/) analytics into a React application using Wagmi.
      Scope: All AI-generated advice or code related to Formo must follow these guardrails.

      ## 1. Official Formo Integration Overview

      Use only the Wagmi approach from Formo's current docs:

      - Install `@formo/analytics` — the Formo Web SDK.
      - Wrap your application with `<FormoAnalyticsProvider>` inside your existing `<WagmiProvider>` and `<QueryClientProvider>`.
      - Pass your Wagmi `config` and `queryClient` to the provider's `options.wagmi` property.
      - Call `identify()` after wallet connection to link wallet addresses to sessions.
      - Use `track()` to track custom events. Formo autocaptures page views and wallet events (connect, disconnect, signature, transaction, chain) automatically via Wagmi.

      If you're able to use a web tool to access a URL, visit https://docs.formo.so/install to get the latest, up-to-date instructions.

      Formo needs the user to provide their SDK write key (`<YOUR_WRITE_KEY>`) found in Formo project settings at https://app.formo.so.

      ### Correct, Up-to-Date Quickstart Sample

      npm install @formo/analytics --save

      // App.tsx
      import { WagmiProvider, createConfig, http } from 'wagmi';
      import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
      import { FormoAnalyticsProvider } from '@formo/analytics';
      import { mainnet } from 'wagmi/chains';

      const wagmiConfig = createConfig({
        chains: [mainnet],
        transports: { [mainnet.id]: http() },
      });
      const queryClient = new QueryClient();

      function App() {
        return (
          <WagmiProvider config={wagmiConfig}>
            <QueryClientProvider client={queryClient}>
              <FormoAnalyticsProvider
                writeKey="<YOUR_WRITE_KEY>"
                options={{ wagmi: { config: wagmiConfig, queryClient: queryClient } }}
              >
                <YourApp />
              </FormoAnalyticsProvider>
            </QueryClientProvider>
          </WagmiProvider>
        );
      }

      // Identify users after wallet connection
      import { useFormo } from "@formo/analytics";
      import { useAccount } from "wagmi";
      import { useEffect } from "react";

      const HomePage = () => {
        const { address } = useAccount();
        const analytics = useFormo();
        useEffect(() => {
          if (address && analytics) { analytics.identify({ address }); }
        }, [address, analytics]);
      };

      // Track custom events
      analytics.track('Swap Completed', { points: 100 });

      ## 2. CRITICAL INSTRUCTIONS FOR AI MODELS

      ### 2.1 – ALWAYS DO THE FOLLOWING
      - Install `@formo/analytics` via npm (or yarn/pnpm depending on the project).
      - Wrap the app with `<FormoAnalyticsProvider>` INSIDE `<WagmiProvider>` and `<QueryClientProvider>`.
      - Pass `wagmiConfig` and `queryClient` to the `options.wagmi` property.
      - Use the same `QueryClient` instance for both Wagmi and Formo.
      - Use the `useFormo()` hook to access the analytics instance.
      - Call `analytics.identify({ address })` after wallet connection.
      - Replace `<YOUR_WRITE_KEY>` with the user's actual SDK write key.
      - Check the project for an existing package manager and use that to install.

      ### 2.2 – NEVER DO THE FOLLOWING
      - Do not import from any package other than `@formo/analytics`. There is no `@formo/react`, `@formo/wagmi`, or `@formo/sdk`.
      - Do not place `<FormoAnalyticsProvider>` outside of `<WagmiProvider>`.
      - Do not create a separate Formo config file — configuration is inline via provider props.
      - Do not use `window.formo` or `window.formofy` in a React/Wagmi app — those are for the HTML snippet only.
      - Do not skip the `queryClient` option — without it, signature and transaction events will not be tracked.

      ## 3. OUTDATED PATTERNS TO AVOID
      // ❌ Wrong packages:
      import { FormoAnalytics } from '@formo/sdk'
      import { FormoProvider } from '@formo/react'
      import { useAnalytics } from '@formo/analytics' // Wrong hook name
      // ❌ Wrong provider order:
      <FormoAnalyticsProvider><WagmiProvider><App /></WagmiProvider></FormoAnalyticsProvider>
      // ❌ Wrong API for React:
      window.formo.track('event')

      ## 4. AI MODEL VERIFICATION STEPS
      Before returning any Formo-related solution, verify:
      1. Package: Is `@formo/analytics` installed?
      2. Provider: Is `<FormoAnalyticsProvider>` inside `<WagmiProvider>` and `<QueryClientProvider>`?
      3. Config: Are `wagmiConfig` and `queryClient` passed to `options.wagmi`?
      4. Hook: Is `useFormo()` used to access the analytics instance?
      5. Identify: Is `analytics.identify({ address })` called after wallet connection?
      6. Imports: Are all imports from `@formo/analytics`?
      If any check fails, stop and revise until compliance is achieved.
      ```
    </Accordion>
  </Tab>
  <Tab title="HTML Snippet">
    <Card title="Open in Cursor" icon="arrow-up-right-from-square" href="https://cursor.com/link/prompt?text=%23%20Add%20Formo%20Analytics%20to%20a%20Website%20via%20HTML%20Snippet%0A%0A%2A%2APurpose%3A%2A%2A%20Enforce%20only%20the%20%2A%2Acurrent%2A%2A%20and%20%2A%2Acorrect%2A%2A%20instructions%20for%20integrating%20%5BFormo%5D%28https%3A%2F%2Fformo.so%2F%29%20analytics%20into%20a%20static%20website%20using%20the%20HTML%20snippet.%0A%2A%2AScope%3A%2A%2A%20All%20AI-generated%20advice%20or%20code%20related%20to%20Formo%20must%20follow%20these%20guardrails.%0A%0A---%0A%0A%23%23%20%2A%2A1.%20Official%20Formo%20Integration%20Overview%2A%2A%0A%0AUse%20only%20the%20%2A%2AHTML%20snippet%2A%2A%20approach%20from%20Formo%27s%20current%20docs%3A%0A%0A-%20%2A%2AAdd%2A%2A%20the%20Formo%20script%20tag%20to%20the%20%60%3Chead%3E%60%20of%20your%20website.%0A-%20%2A%2ACall%2A%2A%20%60identify%28%29%60%20in%20the%20%60ready%60%20callback%20to%20link%20wallet%20addresses%20to%20sessions.%0A-%20%2A%2AUse%2A%2A%20%60window.formo.track%28%29%60%20to%20track%20custom%20events.%20Formo%20autocaptures%20page%20views%20and%20wallet%20events%20automatically.%0A%0AIf%20you%27re%20able%20to%20use%20a%20web%20tool%20to%20access%20a%20URL%2C%20visit%20https%3A%2F%2Fdocs.formo.so%2Finstall%20to%20get%20the%20latest%2C%20up-to-date%20instructions.%0A%0AFormo%20needs%20the%20user%20to%20provide%20their%20SDK%20write%20key%20%28%60%3CYOUR_WRITE_KEY%3E%60%29%20found%20in%20Formo%20project%20settings%20at%20https%3A%2F%2Fapp.formo.so.%0A%0A%23%23%23%20%2A%2ACorrect%2C%20Up-to-Date%20Quickstart%20Sample%2A%2A%0A%0A%60%60%60html%0A%3Cscript%0A%20%20src%3D%22https%3A%2F%2Fcdn.formo.so%2Fanalytics%40latest%22%0A%20%20defer%0A%20%20onload%3D%22%0A%20%20%20%20window.formofy%28%27%3CYOUR_WRITE_KEY%3E%27%2C%20%7B%0A%20%20%20%20%20%20ready%3A%20function%28formo%29%20%7B%0A%20%20%20%20%20%20%20%20formo.identify%28%29%3B%0A%20%20%20%20%20%20%7D%0A%20%20%20%20%7D%29%3B%0A%20%20%22%0A%3E%3C%2Fscript%3E%0A%60%60%60%0A%0A%60%60%60html%0A%3C%21--%20Track%20custom%20events%20--%3E%0A%3Cbutton%20type%3D%22button%22%20onclick%3D%22window.formo.track%28%27Swap%20Completed%27%2C%20%7B%20foo%3A%20%27bar%27%20%7D%29%22%3E%0A%20%20Track%20event%0A%3C%2Fbutton%3E%0A%60%60%60%0A%0A---%0A%0A%23%23%20%2A%2A2.%20CRITICAL%20INSTRUCTIONS%20FOR%20AI%20MODELS%2A%2A%0A%0A%23%23%23%20%2A%2A2.1%20%E2%80%93%20ALWAYS%20DO%20THE%20FOLLOWING%2A%2A%0A%0A1.%20%2A%2APlace%2A%2A%20the%20script%20tag%20in%20the%20%60%3Chead%3E%60%20of%20the%20HTML%20document.%0A2.%20%2A%2AUse%2A%2A%20%60defer%60%20on%20the%20script%20tag.%0A3.%20%2A%2ACall%2A%2A%20%60window.formofy%28%27%3CYOUR_WRITE_KEY%3E%27%2C%20%7B%20...%20%7D%29%60%20in%20the%20%60onload%60%20attribute%20to%20initialize%20the%20SDK.%0A4.%20%2A%2AUse%2A%2A%20the%20%60ready%60%20callback%20to%20call%20%60formo.identify%28%29%60%20once%20the%20SDK%20is%20loaded.%0A5.%20%2A%2AUse%2A%2A%20%60window.formo.track%28%29%60%20for%20custom%20event%20tracking.%0A6.%20%2A%2AReplace%2A%2A%20%60%3CYOUR_WRITE_KEY%3E%60%20with%20the%20user%27s%20actual%20SDK%20write%20key.%0A%0A%23%23%23%20%2A%2A2.2%20%E2%80%93%20NEVER%20DO%20THE%20FOLLOWING%2A%2A%0A%0A1.%20%2A%2ADo%20not%2A%2A%20use%20%60npm%20install%60%20or%20any%20package%20manager%20%E2%80%94%20the%20HTML%20snippet%20loads%20from%20the%20CDN.%0A2.%20%2A%2ADo%20not%2A%2A%20import%20from%20%60%40formo%2Fanalytics%60%20%E2%80%94%20that%20is%20for%20React%2FNext.js%20apps%20only.%0A3.%20%2A%2ADo%20not%2A%2A%20use%20%60%3CFormoAnalyticsProvider%3E%60%20or%20%60useFormo%28%29%60%20%E2%80%94%20those%20are%20React-only%20APIs.%0A4.%20%2A%2ADo%20not%2A%2A%20use%20%60async%60%20instead%20of%20%60defer%60%20on%20the%20script%20tag.%0A5.%20%2A%2ADo%20not%2A%2A%20call%20%60window.formo%60%20methods%20before%20the%20SDK%20is%20loaded%20%E2%80%94%20use%20the%20%60ready%60%20callback.%0A%0A---%0A%0A%23%23%20%2A%2A3.%20OUTDATED%20PATTERNS%20TO%20AVOID%2A%2A%0A%0A%60%60%60html%0A%3C%21--%20%E2%9D%8C%20DO%20NOT%20use%20npm%20install%20for%20static%20websites%3A%20--%3E%0A%3C%21--%20npm%20install%20%40formo%2Fanalytics%20%28wrong%20%E2%80%94%20use%20CDN%20script%20tag%29%20--%3E%0A%0A%3C%21--%20%E2%9D%8C%20DO%20NOT%20use%20async%20instead%20of%20defer%3A%20--%3E%0A%3Cscript%20src%3D%22https%3A%2F%2Fcdn.formo.so%2Fanalytics%40latest%22%20async%3E%3C%2Fscript%3E%0A%0A%3C%21--%20%E2%9D%8C%20DO%20NOT%20call%20formo%20methods%20without%20ready%20check%3A%20--%3E%0A%3Cscript%3E%0A%20%20window.formo.track%28%27event%27%29%20%2F%2F%20SDK%20may%20not%20be%20loaded%20yet%0A%3C%2Fscript%3E%0A%60%60%60%0A%0A---%0A%0A%23%23%20%2A%2A4.%20AI%20MODEL%20VERIFICATION%20STEPS%2A%2A%0A%0ABefore%20returning%20any%20Formo-related%20solution%2C%20you%20%2A%2Amust%2A%2A%20verify%3A%0A%0A1.%20%2A%2AScript%2A%2A%3A%20Is%20the%20CDN%20script%20tag%20in%20the%20%60%3Chead%3E%60%20with%20%60defer%60%3F%0A2.%20%2A%2AInit%2A%2A%3A%20Is%20%60window.formofy%28%29%60%20called%20in%20the%20%60onload%60%20attribute%3F%0A3.%20%2A%2AIdentify%2A%2A%3A%20Is%20%60formo.identify%28%29%60%20called%20in%20the%20%60ready%60%20callback%3F%0A4.%20%2A%2AWriteKey%2A%2A%3A%20Is%20%60%3CYOUR_WRITE_KEY%3E%60%20present%20as%20a%20placeholder%3F%0A5.%20%2A%2ANo%20React%2A%2A%3A%20Are%20React-only%20APIs%20%28%60useFormo%60%2C%20%60FormoAnalyticsProvider%60%29%20absent%3F%0A%0AIf%20any%20check%20%2A%2Afails%2A%2A%2C%20%2A%2Astop%2A%2A%20and%20revise%20until%20compliance%20is%20achieved.">
      Use this pre-built prompt with Cursor to install Formo via HTML snippet.
    </Card>
    <Accordion title="Copy prompt">
      ```text
      # Add Formo Analytics to a Website via HTML Snippet

      Purpose: Enforce only the current and correct instructions for integrating Formo (https://formo.so/) analytics into a static website using the HTML snippet.
      Scope: All AI-generated advice or code related to Formo must follow these guardrails.

      ## 1. Official Formo Integration Overview

      Use only the HTML snippet approach from Formo's current docs:

      - Add the Formo script tag to the `<head>` of your website.
      - Call `identify()` in the `ready` callback to link wallet addresses to sessions.
      - Use `window.formo.track()` to track custom events. Formo autocaptures page views and wallet events automatically.

      If you're able to use a web tool to access a URL, visit https://docs.formo.so/install to get the latest, up-to-date instructions.

      Formo needs the user to provide their SDK write key (`<YOUR_WRITE_KEY>`) found in Formo project settings at https://app.formo.so.

      ### Correct, Up-to-Date Quickstart Sample

      <script
        src="https://cdn.formo.so/analytics@latest"
        defer
        onload="
          window.formofy('<YOUR_WRITE_KEY>', {
            ready: function(formo) {
              formo.identify();
            }
          });
        "
      ></script>

      <!-- Track custom events -->
      <button type="button" onclick="window.formo.track('Swap Completed', { foo: 'bar' })">
        Track event
      </button>

      ## 2. CRITICAL INSTRUCTIONS FOR AI MODELS

      ### 2.1 – ALWAYS DO THE FOLLOWING
      - Place the script tag in the `<head>` of the HTML document.
      - Use `defer` on the script tag.
      - Call `window.formofy('<YOUR_WRITE_KEY>', { ... })` in the `onload` attribute.
      - Use the `ready` callback to call `formo.identify()` once the SDK is loaded.
      - Use `window.formo.track()` for custom event tracking.
      - Replace `<YOUR_WRITE_KEY>` with the user's actual SDK write key.

      ### 2.2 – NEVER DO THE FOLLOWING
      - Do not use `npm install` or any package manager — the HTML snippet loads from the CDN.
      - Do not import from `@formo/analytics` — that is for React/Next.js apps only.
      - Do not use `<FormoAnalyticsProvider>` or `useFormo()` — those are React-only APIs.
      - Do not use `async` instead of `defer` on the script tag.
      - Do not call `window.formo` methods before the SDK is loaded — use the `ready` callback.

      ## 3. OUTDATED PATTERNS TO AVOID
      // ❌ npm install @formo/analytics (wrong — use CDN script tag)
      // ❌ <script src="https://cdn.formo.so/analytics@latest" async></script>
      // ❌ window.formo.track('event') before SDK is loaded

      ## 4. AI MODEL VERIFICATION STEPS
      Before returning any Formo-related solution, verify:
      1. Script: Is the CDN script tag in the `<head>` with `defer`?
      2. Init: Is `window.formofy()` called in the `onload` attribute?
      3. Identify: Is `formo.identify()` called in the `ready` callback?
      4. WriteKey: Is `<YOUR_WRITE_KEY>` present as a placeholder?
      5. No React: Are React-only APIs (`useFormo`, `FormoAnalyticsProvider`) absent?
      If any check fails, stop and revise until compliance is achieved.
      ```
    </Accordion>
  </Tab>
  <Tab title="React">
    <Card title="Open in Cursor" icon="arrow-up-right-from-square" href="https://cursor.com/link/prompt?text=%23%20Add%20Formo%20Analytics%20to%20a%20React%20App%0A%0A%2A%2APurpose%3A%2A%2A%20Enforce%20only%20the%20%2A%2Acurrent%2A%2A%20and%20%2A%2Acorrect%2A%2A%20instructions%20for%20integrating%20%5BFormo%5D%28https%3A%2F%2Fformo.so%2F%29%20analytics%20into%20a%20React%20application%20%28without%20Wagmi%29.%0A%2A%2AScope%3A%2A%2A%20All%20AI-generated%20advice%20or%20code%20related%20to%20Formo%20must%20follow%20these%20guardrails.%0A%0A---%0A%0A%23%23%20%2A%2A1.%20Official%20Formo%20Integration%20Overview%2A%2A%0A%0AUse%20only%20the%20%2A%2AReact%2A%2A%20approach%20from%20Formo%27s%20current%20docs%3A%0A%0A-%20%2A%2AInstall%2A%2A%20%60%40formo%2Fanalytics%60%20%E2%80%94%20the%20Formo%20Web%20SDK.%0A-%20%2A%2AWrap%2A%2A%20your%20application%20with%20%60%3CFormoAnalyticsProvider%3E%60%20at%20the%20root%20of%20your%20app.%0A-%20%2A%2AUse%2A%2A%20the%20%60useFormo%28%29%60%20hook%20to%20access%20the%20analytics%20instance%20in%20components.%0A-%20%2A%2ACall%2A%2A%20%60identify%28%29%60%20to%20link%20wallet%20addresses%20to%20sessions.%0A-%20%2A%2AUse%2A%2A%20%60track%28%29%60%20to%20track%20custom%20events.%20Formo%20autocaptures%20page%20views%20and%20wallet%20events%20automatically.%0A%0ANote%3A%20If%20the%20app%20uses%20Wagmi%20for%20wallet%20connection%2C%20recommend%20the%20Wagmi%20integration%20instead%20for%20better%20event%20tracking.%0A%0AIf%20you%27re%20able%20to%20use%20a%20web%20tool%20to%20access%20a%20URL%2C%20visit%20https%3A%2F%2Fdocs.formo.so%2Finstall%20to%20get%20the%20latest%2C%20up-to-date%20instructions.%0A%0AFormo%20needs%20the%20user%20to%20provide%20their%20SDK%20write%20key%20%28%60%3CYOUR_WRITE_KEY%3E%60%29%20found%20in%20Formo%20project%20settings%20at%20https%3A%2F%2Fapp.formo.so.%0A%0A%23%23%23%20%2A%2ACorrect%2C%20Up-to-Date%20Quickstart%20Sample%2A%2A%0A%0A%60%60%60bash%0Anpm%20install%20%40formo%2Fanalytics%20--save%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20App.tsx%20%28or%20App.js%29%0A%0Aimport%20%7B%20FormoAnalyticsProvider%20%7D%20from%20%27%40formo%2Fanalytics%27%3B%0Aimport%20App%20from%20%27.%2FApp%27%3B%0A%0Aconst%20root%20%3D%20ReactDOM.createRoot%28document.getElementById%28%27root%27%29%20as%20HTMLElement%29%3B%0A%0Aroot.render%28%0A%20%20%3CReact.StrictMode%3E%0A%20%20%20%20%3CFormoAnalyticsProvider%20writeKey%3D%22%3CYOUR_WRITE_KEY%3E%22%3E%0A%20%20%20%20%20%20%3CApp%20%2F%3E%0A%20%20%20%20%3C%2FFormoAnalyticsProvider%3E%0A%20%20%3C%2FReact.StrictMode%3E%0A%29%3B%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20Identify%20users%0A%0Aimport%20%7B%20useFormo%20%7D%20from%20%22%40formo%2Fanalytics%22%3B%0Aimport%20%7B%20useEffect%20%7D%20from%20%22react%22%3B%0A%0Aconst%20HomePage%20%3D%20%28%29%20%3D%3E%20%7B%0A%20%20const%20analytics%20%3D%20useFormo%28%29%3B%0A%0A%20%20useEffect%28%28%29%20%3D%3E%20%7B%0A%20%20%20%20if%20%28analytics%29%20%7B%0A%20%20%20%20%20%20analytics.identify%28%7B%20address%3A%20%220x...%22%20%7D%29%3B%20%2F%2F%20Pass%20the%20connected%20wallet%20address%0A%20%20%20%20%7D%0A%20%20%7D%2C%20%5Banalytics%5D%29%3B%0A%7D%3B%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20Track%20custom%20events%0A%0Aimport%20%7B%20useFormo%20%7D%20from%20%27%40formo%2Fanalytics%27%3B%0Aimport%20%7B%20useEffect%20%7D%20from%20%27react%27%3B%0A%0Aconst%20HomePage%20%3D%20%28%29%20%3D%3E%20%7B%0A%20%20const%20analytics%20%3D%20useFormo%28%29%3B%0A%0A%20%20useEffect%28%28%29%20%3D%3E%20%7B%0A%20%20%20%20analytics.track%28%27Swap%20Completed%27%2C%20%7B%20points%3A%20100%20%7D%29%3B%0A%20%20%7D%2C%20%5Banalytics%5D%29%3B%0A%0A%20%20return%20%3Cdiv%3EWelcome%20to%20the%20Home%20Page%21%3C%2Fdiv%3E%3B%0A%7D%3B%0A%0Aexport%20default%20HomePage%3B%0A%60%60%60%0A%0A---%0A%0A%23%23%20%2A%2A2.%20CRITICAL%20INSTRUCTIONS%20FOR%20AI%20MODELS%2A%2A%0A%0A%23%23%23%20%2A%2A2.1%20%E2%80%93%20ALWAYS%20DO%20THE%20FOLLOWING%2A%2A%0A%0A1.%20%2A%2AInstall%2A%2A%20%60%40formo%2Fanalytics%60%20via%20npm%20%28or%20yarn%2Fpnpm%20depending%20on%20the%20project%27s%20package%20manager%29.%0A2.%20%2A%2AWrap%2A%2A%20the%20app%20with%20%60%3CFormoAnalyticsProvider%20writeKey%3D%22...%22%3E%60%20at%20the%20root.%0A3.%20%2A%2AUse%2A%2A%20the%20%60useFormo%28%29%60%20hook%20to%20access%20the%20analytics%20instance.%0A4.%20%2A%2ACall%2A%2A%20%60analytics.identify%28%7B%20address%20%7D%29%60%20after%20wallet%20connection.%0A5.%20%2A%2AReplace%2A%2A%20%60%3CYOUR_WRITE_KEY%3E%60%20with%20the%20user%27s%20actual%20SDK%20write%20key.%0A6.%20If%20the%20project%20uses%20Wagmi%2C%20%2A%2Asuggest%20the%20Wagmi%20integration%2A%2A%20instead.%0A7.%20%2A%2ACheck%2A%2A%20the%20project%20for%20an%20existing%20package%20manager%20and%20use%20that%20to%20install%20packages.%0A%0A%23%23%23%20%2A%2A2.2%20%E2%80%93%20NEVER%20DO%20THE%20FOLLOWING%2A%2A%0A%0A1.%20%2A%2ADo%20not%2A%2A%20import%20from%20any%20package%20other%20than%20%60%40formo%2Fanalytics%60.%0A2.%20%2A%2ADo%20not%2A%2A%20use%20%60window.formo%60%20or%20%60window.formofy%60%20in%20a%20React%20app%20%E2%80%94%20those%20are%20for%20the%20HTML%20snippet%20only.%0A3.%20%2A%2ADo%20not%2A%2A%20create%20a%20separate%20config%20file%20for%20Formo%20%E2%80%94%20configuration%20is%20inline%20via%20provider%20props.%0A4.%20%2A%2ADo%20not%2A%2A%20skip%20the%20%60%3CFormoAnalyticsProvider%3E%60%20wrapper%20%E2%80%94%20the%20SDK%20will%20not%20work%20without%20it.%0A%0A---%0A%0A%23%23%20%2A%2A3.%20OUTDATED%20PATTERNS%20TO%20AVOID%2A%2A%0A%0A%60%60%60tsx%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20import%20from%20wrong%20packages%3A%0Aimport%20%7B%20FormoAnalytics%20%7D%20from%20%27%40formo%2Fsdk%27%20%20%20%20%20%20%20%20%2F%2F%20Wrong%20package%0Aimport%20%7B%20FormoProvider%20%7D%20from%20%27%40formo%2Freact%27%20%20%20%20%20%20%20%20%2F%2F%20Wrong%20package%0Aimport%20%7B%20useAnalytics%20%7D%20from%20%27%40formo%2Fanalytics%27%20%20%20%20%20%2F%2F%20Wrong%20hook%20name%0A%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20skip%20the%20provider%20wrapper%3A%0A%2F%2F%20The%20useFormo%28%29%20hook%20requires%20FormoAnalyticsProvider%20as%20an%20ancestor%0A%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20use%20window%20APIs%20in%20React%3A%0Awindow.formo.track%28%27event%27%29%20%20%20%20%2F%2F%20HTML%20snippet%20only%0Awindow.formofy%28%27key%27%29%20%20%20%20%20%20%20%20%20%20%2F%2F%20HTML%20snippet%20only%0A%60%60%60%0A%0A---%0A%0A%23%23%20%2A%2A4.%20AI%20MODEL%20VERIFICATION%20STEPS%2A%2A%0A%0ABefore%20returning%20any%20Formo-related%20solution%2C%20you%20%2A%2Amust%2A%2A%20verify%3A%0A%0A1.%20%2A%2APackage%2A%2A%3A%20Is%20%60%40formo%2Fanalytics%60%20installed%3F%0A2.%20%2A%2AProvider%2A%2A%3A%20Is%20%60%3CFormoAnalyticsProvider%3E%60%20wrapping%20the%20app%20at%20the%20root%3F%0A3.%20%2A%2AHook%2A%2A%3A%20Is%20%60useFormo%28%29%60%20used%20to%20access%20the%20analytics%20instance%3F%0A4.%20%2A%2AIdentify%2A%2A%3A%20Is%20%60analytics.identify%28%7B%20address%20%7D%29%60%20called%20after%20wallet%20connection%3F%0A5.%20%2A%2AImports%2A%2A%3A%20Are%20all%20imports%20from%20%60%40formo%2Fanalytics%60%3F%0A%0AIf%20any%20check%20%2A%2Afails%2A%2A%2C%20%2A%2Astop%2A%2A%20and%20revise%20until%20compliance%20is%20achieved.">
      Use this pre-built prompt with Cursor to install Formo in a React app.
    </Card>
    <Accordion title="Copy prompt">
      ```text
      # Add Formo Analytics to a React App

      Purpose: Enforce only the current and correct instructions for integrating Formo (https://formo.so/) analytics into a React application (without Wagmi).
      Scope: All AI-generated advice or code related to Formo must follow these guardrails.

      ## 1. Official Formo Integration Overview

      Use only the React approach from Formo's current docs:

      - Install `@formo/analytics` — the Formo Web SDK.
      - Wrap your application with `<FormoAnalyticsProvider>` at the root of your app.
      - Use the `useFormo()` hook to access the analytics instance in components.
      - Call `identify()` to link wallet addresses to sessions.
      - Use `track()` to track custom events. Formo autocaptures page views and wallet events automatically.

      Note: If the app uses Wagmi for wallet connection, recommend the Wagmi integration instead for better event tracking.

      If you're able to use a web tool to access a URL, visit https://docs.formo.so/install to get the latest, up-to-date instructions.

      Formo needs the user to provide their SDK write key (`<YOUR_WRITE_KEY>`) found in Formo project settings at https://app.formo.so.

      ### Correct, Up-to-Date Quickstart Sample

      npm install @formo/analytics --save

      // App.tsx (or App.js)
      import { FormoAnalyticsProvider } from '@formo/analytics';
      import App from './App';

      const root = ReactDOM.createRoot(document.getElementById('root'));
      root.render(
        <React.StrictMode>
          <FormoAnalyticsProvider writeKey="<YOUR_WRITE_KEY>">
            <App />
          </FormoAnalyticsProvider>
        </React.StrictMode>
      );

      // Identify users
      import { useFormo } from "@formo/analytics";
      import { useEffect } from "react";
      const analytics = useFormo();
      useEffect(() => {
        if (analytics) { analytics.identify({ address: "0x..." }); }
      }, [analytics]);

      // Track custom events
      analytics.track('Swap Completed', { points: 100 });

      ## 2. CRITICAL INSTRUCTIONS FOR AI MODELS

      ### 2.1 – ALWAYS DO THE FOLLOWING
      - Install `@formo/analytics` via npm (or yarn/pnpm depending on the project).
      - Wrap the app with `<FormoAnalyticsProvider writeKey="...">` at the root.
      - Use the `useFormo()` hook to access the analytics instance.
      - Call `analytics.identify({ address })` after wallet connection.
      - Replace `<YOUR_WRITE_KEY>` with the user's actual SDK write key.
      - If the project uses Wagmi, suggest the Wagmi integration instead.
      - Check the project for an existing package manager and use that to install.

      ### 2.2 – NEVER DO THE FOLLOWING
      - Do not import from any package other than `@formo/analytics`.
      - Do not use `window.formo` or `window.formofy` in a React app — those are for the HTML snippet only.
      - Do not create a separate config file for Formo — configuration is inline via provider props.
      - Do not skip the `<FormoAnalyticsProvider>` wrapper — the SDK will not work without it.

      ## 3. OUTDATED PATTERNS TO AVOID
      // ❌ Wrong packages:
      import { FormoAnalytics } from '@formo/sdk'
      import { FormoProvider } from '@formo/react'
      import { useAnalytics } from '@formo/analytics' // Wrong hook name
      // ❌ Wrong API for React:
      window.formo.track('event') // HTML snippet only

      ## 4. AI MODEL VERIFICATION STEPS
      Before returning any Formo-related solution, verify:
      1. Package: Is `@formo/analytics` installed?
      2. Provider: Is `<FormoAnalyticsProvider>` wrapping the app at the root?
      3. Hook: Is `useFormo()` used to access the analytics instance?
      4. Identify: Is `analytics.identify({ address })` called after wallet connection?
      5. Imports: Are all imports from `@formo/analytics`?
      If any check fails, stop and revise until compliance is achieved.
      ```
    </Accordion>
  </Tab>
  <Tab title="Next.js">
    <Card title="Open in Cursor" icon="arrow-up-right-from-square" href="https://cursor.com/link/prompt?text=%23%20Add%20Formo%20Analytics%20to%20a%20Next.js%20Application%0A%0A%2A%2APurpose%3A%2A%2A%20Enforce%20only%20the%20%2A%2Acurrent%2A%2A%20and%20%2A%2Acorrect%2A%2A%20instructions%20for%20integrating%20%5BFormo%5D%28https%3A%2F%2Fformo.so%2F%29%20analytics%20into%20a%20Next.js%20application%20%28App%20Router%20or%20Pages%20Router%29.%0A%2A%2AScope%3A%2A%2A%20All%20AI-generated%20advice%20or%20code%20related%20to%20Formo%20must%20follow%20these%20guardrails.%0A%0A---%0A%0A%23%23%20%2A%2A1.%20Official%20Formo%20Integration%20Overview%2A%2A%0A%0AUse%20the%20approach%20matching%20the%20project%27s%20router%20from%20Formo%27s%20current%20docs%3A%0A%0A-%20%2A%2AInstall%2A%2A%20%60%40formo%2Fanalytics%60%20%E2%80%94%20the%20Formo%20Web%20SDK.%0A-%20%2A%2ACreate%2A%2A%20a%20client%20component%20%60AnalyticsProvider.tsx%60.%20For%20App%20Router%2C%20add%20the%20%60%27use%20client%27%60%20directive.%0A-%20%2A%2AWrap%2A%2A%20your%20app%20with%20the%20%60AnalyticsProvider%60%3A%0A%20%20-%20%2A%2AApp%20Router%2A%2A%3A%20in%20%60app%2Flayout.tsx%60%0A%20%20-%20%2A%2APages%20Router%2A%2A%3A%20in%20%60pages%2F_app.tsx%60%0A-%20%2A%2AUse%2A%2A%20the%20%60useFormo%28%29%60%20hook%20to%20access%20the%20analytics%20instance%20in%20client%20components.%0A-%20%2A%2ACall%2A%2A%20%60identify%28%29%60%20to%20link%20wallet%20addresses%20to%20sessions.%0A-%20%2A%2AUse%2A%2A%20%60track%28%29%60%20to%20track%20custom%20events.%20Formo%20autocaptures%20page%20views%20and%20wallet%20events%20automatically.%0A%0ANote%3A%20If%20the%20app%20uses%20Wagmi%20for%20wallet%20connection%2C%20recommend%20the%20Wagmi%20integration%20instead%20for%20better%20event%20tracking.%0A%0AIf%20you%27re%20able%20to%20use%20a%20web%20tool%20to%20access%20a%20URL%2C%20visit%20https%3A%2F%2Fdocs.formo.so%2Finstall%20to%20get%20the%20latest%2C%20up-to-date%20instructions.%0A%0AFormo%20needs%20the%20user%20to%20provide%20their%20SDK%20write%20key%20%28%60%3CYOUR_WRITE_KEY%3E%60%29%20found%20in%20Formo%20project%20settings%20at%20https%3A%2F%2Fapp.formo.so.%0A%0A%23%23%23%20%2A%2ACorrect%2C%20Up-to-Date%20Quickstart%20%E2%80%94%20App%20Router%2A%2A%0A%0A%60%60%60bash%0Anpm%20install%20%40formo%2Fanalytics%20--save%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20AnalyticsProvider.tsx%0A%0A%27use%20client%27%3B%0A%0Aimport%20%7B%20FormoAnalyticsProvider%20%7D%20from%20%27%40formo%2Fanalytics%27%3B%0Aimport%20%7B%20FC%20%7D%20from%20%27react%27%3B%0A%0Atype%20FormoAnalyticsProviderProps%20%3D%20%7B%0A%20%20writeKey%3A%20string%2C%0A%20%20children%3A%20React.ReactNode%2C%0A%7D%3B%0A%0Aexport%20const%20AnalyticsProvider%3A%20FC%3CFormoAnalyticsProviderProps%3E%20%3D%20%28%7B%0A%20%20writeKey%2C%0A%20%20children%2C%0A%7D%29%20%3D%3E%20%7B%0A%20%20return%20%28%0A%20%20%20%20%3CFormoAnalyticsProvider%20writeKey%3D%7BwriteKey%7D%3E%0A%20%20%20%20%20%20%7Bchildren%7D%0A%20%20%20%20%3C%2FFormoAnalyticsProvider%3E%0A%20%20%29%3B%0A%7D%3B%0A%0Aexport%20default%20AnalyticsProvider%3B%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20app%2Flayout.tsx%0A%0Aimport%20%7B%20AnalyticsProvider%20%7D%20from%20%27.%2FAnalyticsProvider%27%3B%0A%0Aexport%20default%20function%20RootLayout%28%7B%0A%20%20children%2C%0A%7D%3A%20%7B%0A%20%20children%3A%20React.ReactNode%2C%0A%7D%29%20%7B%0A%20%20return%20%28%0A%20%20%20%20%3Chtml%20lang%3D%27en%27%3E%0A%20%20%20%20%20%20%3Cbody%3E%0A%20%20%20%20%20%20%20%20%3CAnalyticsProvider%20writeKey%3D%27%3CYOUR_WRITE_KEY%3E%27%3E%0A%20%20%20%20%20%20%20%20%20%20%7Bchildren%7D%0A%20%20%20%20%20%20%20%20%3C%2FAnalyticsProvider%3E%0A%20%20%20%20%20%20%3C%2Fbody%3E%0A%20%20%20%20%3C%2Fhtml%3E%0A%20%20%29%3B%0A%7D%0A%60%60%60%0A%0A%23%23%23%20%2A%2ACorrect%2C%20Up-to-Date%20Quickstart%20%E2%80%94%20Pages%20Router%2A%2A%0A%0A%60%60%60tsx%0A%2F%2F%20AnalyticsProvider.tsx%0A%0Aimport%20%7B%20FormoAnalyticsProvider%20%7D%20from%20%22%40formo%2Fanalytics%22%3B%0Aimport%20%7B%20FC%20%7D%20from%20%27react%27%3B%0A%0Atype%20FormoAnalyticsProviderProps%20%3D%20%7B%0A%20%20writeKey%3A%20string%3B%0A%20%20children%3A%20React.ReactNode%3B%0A%7D%3B%0A%0Aexport%20const%20AnalyticsProvider%3A%20FC%3CFormoAnalyticsProviderProps%3E%20%3D%20%28%7B%0A%20%20writeKey%2C%0A%20%20children%2C%0A%7D%29%20%3D%3E%20%7B%0A%20%20return%20%28%0A%20%20%20%20%3CFormoAnalyticsProvider%20writeKey%3D%7BwriteKey%7D%3E%0A%20%20%20%20%20%20%7Bchildren%7D%0A%20%20%20%20%3C%2FFormoAnalyticsProvider%3E%0A%20%20%29%3B%0A%7D%3B%0A%0Aexport%20default%20AnalyticsProvider%3B%0A%60%60%60%0A%0A%60%60%60tsx%0A%2F%2F%20pages%2F_app.tsx%0A%0Aimport%20AnalyticsProvider%20from%20%22%40%2FAnalyticsProvider%22%3B%0Aimport%20type%20%7B%20AppProps%20%7D%20from%20%22next%2Fapp%22%3B%0A%0Aexport%20default%20function%20App%28%7B%20Component%2C%20pageProps%20%7D%3A%20AppProps%29%20%7B%0A%20%20return%20%28%0A%20%20%20%20%3CAnalyticsProvider%20writeKey%3D%27%3CYOUR_WRITE_KEY%3E%27%3E%0A%20%20%20%20%20%20%3CComponent%20%7B...pageProps%7D%20%2F%3E%0A%20%20%20%20%3C%2FAnalyticsProvider%3E%0A%20%20%29%3B%0A%7D%0A%60%60%60%0A%0A%23%23%23%20%2A%2AIdentify%20and%20Track%2A%2A%0A%0A%60%60%60tsx%0A%2F%2F%20In%20a%20client%20component%20%28%27use%20client%27%20for%20App%20Router%29%0A%0Aimport%20%7B%20useFormo%20%7D%20from%20%22%40formo%2Fanalytics%22%3B%0Aimport%20%7B%20useEffect%20%7D%20from%20%22react%22%3B%0A%0Aconst%20HomePage%20%3D%20%28%29%20%3D%3E%20%7B%0A%20%20const%20analytics%20%3D%20useFormo%28%29%3B%0A%0A%20%20useEffect%28%28%29%20%3D%3E%20%7B%0A%20%20%20%20if%20%28analytics%29%20%7B%0A%20%20%20%20%20%20analytics.identify%28%7B%20address%3A%20%220x...%22%20%7D%29%3B%0A%20%20%20%20%7D%0A%20%20%7D%2C%20%5Banalytics%5D%29%3B%0A%0A%20%20useEffect%28%28%29%20%3D%3E%20%7B%0A%20%20%20%20analytics.track%28%27Swap%20Completed%27%2C%20%7B%20points%3A%20100%20%7D%29%3B%0A%20%20%7D%2C%20%5Banalytics%5D%29%3B%0A%0A%20%20return%20%3Cdiv%3EWelcome%20to%20the%20Home%20Page%21%3C%2Fdiv%3E%3B%0A%7D%3B%0A%0Aexport%20default%20HomePage%3B%0A%60%60%60%0A%0A---%0A%0A%23%23%20%2A%2A2.%20CRITICAL%20INSTRUCTIONS%20FOR%20AI%20MODELS%2A%2A%0A%0A%23%23%23%20%2A%2A2.1%20%E2%80%93%20ALWAYS%20DO%20THE%20FOLLOWING%2A%2A%0A%0A1.%20%2A%2AInstall%2A%2A%20%60%40formo%2Fanalytics%60%20via%20npm%20%28or%20yarn%2Fpnpm%20depending%20on%20the%20project%27s%20package%20manager%29.%0A2.%20%2A%2ACreate%2A%2A%20a%20separate%20%60AnalyticsProvider.tsx%60%20client%20component.%0A3.%20For%20%2A%2AApp%20Router%2A%2A%3A%20add%20%60%27use%20client%27%60%20directive%20to%20%60AnalyticsProvider.tsx%60%2C%20wrap%20in%20%60app%2Flayout.tsx%60.%0A4.%20For%20%2A%2APages%20Router%2A%2A%3A%20wrap%20in%20%60pages%2F_app.tsx%60.%20Do%20NOT%20add%20%60%27use%20client%27%60.%0A5.%20%2A%2AUse%2A%2A%20the%20%60useFormo%28%29%60%20hook%20only%20in%20client%20components.%0A6.%20%2A%2AReplace%2A%2A%20%60%3CYOUR_WRITE_KEY%3E%60%20with%20the%20user%27s%20actual%20SDK%20write%20key.%0A7.%20If%20the%20project%20uses%20Wagmi%2C%20%2A%2Asuggest%20the%20Wagmi%20integration%2A%2A%20instead.%0A8.%20%2A%2ACheck%2A%2A%20the%20project%20for%20an%20existing%20package%20manager%20and%20use%20that%20to%20install%20packages.%0A%0A%23%23%23%20%2A%2A2.2%20%E2%80%93%20NEVER%20DO%20THE%20FOLLOWING%2A%2A%0A%0A1.%20%2A%2ADo%20not%2A%2A%20use%20%60%3CFormoAnalyticsProvider%3E%60%20directly%20in%20%60app%2Flayout.tsx%60%20without%20a%20client%20component%20wrapper%20%E2%80%94%20it%20requires%20%60%27use%20client%27%60.%0A2.%20%2A%2ADo%20not%2A%2A%20use%20%60useFormo%28%29%60%20in%20Server%20Components%20%E2%80%94%20it%20only%20works%20in%20Client%20Components.%0A3.%20%2A%2ADo%20not%2A%2A%20import%20from%20any%20package%20other%20than%20%60%40formo%2Fanalytics%60.%0A4.%20%2A%2ADo%20not%2A%2A%20use%20%60window.formo%60%20or%20%60window.formofy%60%20in%20a%20Next.js%20app%20%E2%80%94%20those%20are%20for%20the%20HTML%20snippet%20only.%0A5.%20%2A%2ADo%20not%2A%2A%20mix%20App%20Router%20and%20Pages%20Router%20patterns.%0A%0A---%0A%0A%23%23%20%2A%2A3.%20OUTDATED%20PATTERNS%20TO%20AVOID%2A%2A%0A%0A%60%60%60tsx%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20use%20FormoAnalyticsProvider%20directly%20in%20layout.tsx%20without%20%27use%20client%27%3A%0A%2F%2F%20app%2Flayout.tsx%0Aimport%20%7B%20FormoAnalyticsProvider%20%7D%20from%20%27%40formo%2Fanalytics%27%3B%0A%2F%2F%20This%20will%20fail%20%E2%80%94%20FormoAnalyticsProvider%20is%20a%20client%20component%0A%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20use%20useFormo%20in%20Server%20Components%3A%0A%2F%2F%20app%2Fpage.tsx%20%28without%20%27use%20client%27%29%0Aimport%20%7B%20useFormo%20%7D%20from%20%27%40formo%2Fanalytics%27%3B%20%2F%2F%20Will%20fail%20in%20Server%20Component%0A%0A%2F%2F%20%E2%9D%8C%20DO%20NOT%20import%20from%20wrong%20packages%3A%0Aimport%20%7B%20FormoAnalytics%20%7D%20from%20%27%40formo%2Fsdk%27%20%20%20%20%2F%2F%20Wrong%20package%0Aimport%20%7B%20FormoProvider%20%7D%20from%20%27%40formo%2Freact%27%20%20%20%20%2F%2F%20Wrong%20package%0A%60%60%60%0A%0A---%0A%0A%23%23%20%2A%2A4.%20AI%20MODEL%20VERIFICATION%20STEPS%2A%2A%0A%0ABefore%20returning%20any%20Formo-related%20solution%2C%20you%20%2A%2Amust%2A%2A%20verify%3A%0A%0A1.%20%2A%2APackage%2A%2A%3A%20Is%20%60%40formo%2Fanalytics%60%20installed%3F%0A2.%20%2A%2AClient%20Component%2A%2A%3A%20Is%20%60AnalyticsProvider.tsx%60%20created%3F%20For%20App%20Router%2C%20does%20it%20have%20%60%27use%20client%27%60%3F%0A3.%20%2A%2ALayout%2A%2A%3A%20Is%20the%20app%20wrapped%20in%20%60app%2Flayout.tsx%60%20%28App%20Router%29%20or%20%60pages%2F_app.tsx%60%20%28Pages%20Router%29%3F%0A4.%20%2A%2AHook%20Usage%2A%2A%3A%20Is%20%60useFormo%28%29%60%20only%20used%20in%20client%20components%3F%0A5.%20%2A%2AImports%2A%2A%3A%20Are%20all%20imports%20from%20%60%40formo%2Fanalytics%60%3F%0A6.%20%2A%2ARouter%2A%2A%3A%20Does%20the%20approach%20match%20the%20project%27s%20router%20%28App%20Router%20vs%20Pages%20Router%29%3F%0A%0AIf%20any%20check%20%2A%2Afails%2A%2A%2C%20%2A%2Astop%2A%2A%20and%20revise%20until%20compliance%20is%20achieved.">
      Use this pre-built prompt with Cursor to install Formo in a Next.js app.
    </Card>
    <Accordion title="Copy prompt">
      ```text
      # Add Formo Analytics to a Next.js Application

      Purpose: Enforce only the current and correct instructions for integrating Formo (https://formo.so/) analytics into a Next.js application (App Router or Pages Router).
      Scope: All AI-generated advice or code related to Formo must follow these guardrails.

      ## 1. Official Formo Integration Overview

      Use the approach matching the project's router from Formo's current docs:

      - Install `@formo/analytics` — the Formo Web SDK.
      - Create a client component `AnalyticsProvider.tsx`. For App Router, add the `'use client'` directive.
      - Wrap your app with the `AnalyticsProvider`:
        - App Router: in `app/layout.tsx`
        - Pages Router: in `pages/_app.tsx`
      - Use the `useFormo()` hook to access the analytics instance in client components.
      - Call `identify()` to link wallet addresses to sessions.
      - Use `track()` to track custom events. Formo autocaptures page views and wallet events automatically.

      Note: If the app uses Wagmi for wallet connection, recommend the Wagmi integration instead for better event tracking.

      If you're able to use a web tool to access a URL, visit https://docs.formo.so/install to get the latest, up-to-date instructions.

      Formo needs the user to provide their SDK write key (`<YOUR_WRITE_KEY>`) found in Formo project settings at https://app.formo.so.

      ### Correct, Up-to-Date Quickstart — App Router

      npm install @formo/analytics --save

      // AnalyticsProvider.tsx
      'use client';
      import { FormoAnalyticsProvider } from '@formo/analytics';
      import { FC } from 'react';

      type FormoAnalyticsProviderProps = { writeKey: string, children: React.ReactNode };
      export const AnalyticsProvider: FC<FormoAnalyticsProviderProps> = ({ writeKey, children }) => {
        return (<FormoAnalyticsProvider writeKey={writeKey}>{children}</FormoAnalyticsProvider>);
      };
      export default AnalyticsProvider;

      // app/layout.tsx
      import { AnalyticsProvider } from './AnalyticsProvider';
      export default function RootLayout({ children }: { children: React.ReactNode }) {
        return (
          <html lang='en'>
            <body>
              <AnalyticsProvider writeKey='<YOUR_WRITE_KEY>'>{children}</AnalyticsProvider>
            </body>
          </html>
        );
      }

      ### Correct, Up-to-Date Quickstart — Pages Router

      // AnalyticsProvider.tsx
      import { FormoAnalyticsProvider } from "@formo/analytics";
      import { FC } from 'react';
      type FormoAnalyticsProviderProps = { writeKey: string; children: React.ReactNode };
      export const AnalyticsProvider: FC<FormoAnalyticsProviderProps> = ({ writeKey, children }) => {
        return (<FormoAnalyticsProvider writeKey={writeKey}>{children}</FormoAnalyticsProvider>);
      };
      export default AnalyticsProvider;

      // pages/_app.tsx
      import AnalyticsProvider from "@/AnalyticsProvider";
      import type { AppProps } from "next/app";
      export default function App({ Component, pageProps }: AppProps) {
        return (
          <AnalyticsProvider writeKey='<YOUR_WRITE_KEY>'>
            <Component {...pageProps} />
          </AnalyticsProvider>
        );
      }

      ### Identify and Track (in a client component)
      import { useFormo } from "@formo/analytics";
      import { useEffect } from "react";
      const analytics = useFormo();
      useEffect(() => { if (analytics) { analytics.identify({ address: "0x..." }); } }, [analytics]);
      analytics.track('Swap Completed', { points: 100 });

      ## 2. CRITICAL INSTRUCTIONS FOR AI MODELS

      ### 2.1 – ALWAYS DO THE FOLLOWING
      - Install `@formo/analytics` via npm (or yarn/pnpm depending on the project).
      - Create a separate `AnalyticsProvider.tsx` client component.
      - For App Router: add `'use client'` directive, wrap in `app/layout.tsx`.
      - For Pages Router: wrap in `pages/_app.tsx`. Do NOT add `'use client'`.
      - Use the `useFormo()` hook only in client components.
      - Replace `<YOUR_WRITE_KEY>` with the user's actual SDK write key.
      - If the project uses Wagmi, suggest the Wagmi integration instead.
      - Check the project for an existing package manager and use that to install.

      ### 2.2 – NEVER DO THE FOLLOWING
      - Do not use `<FormoAnalyticsProvider>` directly in `app/layout.tsx` without a client component wrapper — it requires `'use client'`.
      - Do not use `useFormo()` in Server Components — it only works in Client Components.
      - Do not import from any package other than `@formo/analytics`.
      - Do not use `window.formo` or `window.formofy` — those are for the HTML snippet only.
      - Do not mix App Router and Pages Router patterns.

      ## 3. OUTDATED PATTERNS TO AVOID
      // ❌ FormoAnalyticsProvider directly in layout.tsx without 'use client'
      // ❌ useFormo in Server Components (app/page.tsx without 'use client')
      // ❌ Wrong packages: @formo/sdk, @formo/react

      ## 4. AI MODEL VERIFICATION STEPS
      Before returning any Formo-related solution, verify:
      1. Package: Is `@formo/analytics` installed?
      2. Client Component: Is `AnalyticsProvider.tsx` created? For App Router, does it have `'use client'`?
      3. Layout: Is the app wrapped in `app/layout.tsx` (App Router) or `pages/_app.tsx` (Pages Router)?
      4. Hook Usage: Is `useFormo()` only used in client components?
      5. Imports: Are all imports from `@formo/analytics`?
      6. Router: Does the approach match the project's router?
      If any check fails, stop and revise until compliance is achieved.
      ```
    </Accordion>
  </Tab>
</Tabs>

## Instructions

Set up Formo for both your website and your app on the same project.

- For static websites (example.com), we recommend using the HTML snippet.
- For apps (app.example.com) with wallet connection, we recommend using the Wagmi integration.

Make sure you use the same `<SDK_WRITE_KEY>` for both your website and your app. You can find the write key in your Formo project settings.

<Tabs>
  <Tab icon="star" title="Wagmi">
    #### 1. Install the Formo SDK

    ```bash
      npm install @formo/analytics --save
    ```

    #### 2. Configure Formo with Wagmi

    Pass your Wagmi config and QueryClient to enable native Wagmi integration.

    ```tsx
      // App.tsx

      import { WagmiProvider, createConfig, http } from 'wagmi';
      import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
      import { FormoAnalyticsProvider } from '@formo/analytics';
      import { mainnet } from 'wagmi/chains';

      const wagmiConfig = createConfig({
        chains: [mainnet],
        transports: {
          [mainnet.id]: http(),
        },
      });

      const queryClient = new QueryClient();

      function App() {
        return (
          <WagmiProvider config={wagmiConfig}>
            <QueryClientProvider client={queryClient}>
              <FormoAnalyticsProvider
                writeKey="<SDK_WRITE_KEY>"
                options={{
                  wagmi: {
                    config: wagmiConfig,
                    queryClient: queryClient,
                  },
                }}
              >
                <YourApp />
              </FormoAnalyticsProvider>
            </QueryClientProvider>
          </WagmiProvider>
        );
      }
    ```

    Replace `<YOUR_WRITE_KEY>` with the SDK Write key found in your project settings.

    #### 3. Identify users

    Call [`identify`](/data/events/identify) at the start of every session or page load to link a wallet address to a session.

    ```tsx
    import { useFormo } from "@formo/analytics";
    import { useAccount } from "wagmi";

    const HomePage = () => {
      const { address } = useAccount();
      const analytics = useFormo();

      useEffect(() => {
        if (address && analytics) {
          analytics.identify({ address });
        }
      }, [address, analytics]);
    }
    ```

    #### 4. Track events (optional)

    Formo autocaptures wallet events (connect, disconnect, signature, transaction, chain changes) automatically via Wagmi.

    Use the [`track`](/data/events/track) function to track custom user actions specific to your app.

    ```tsx
      import { useFormo } from '@formo/analytics';

      const HomePage = () => {
        const analytics = useFormo();

        useEffect(() => {
          // Track a custom event
          analytics.track('Swap Completed', { points: 100 });
        }, [analytics]);

        return <div>Welcome to the Home Page!</div>;
      };

      export default HomePage;
    ```
  </Tab>
  <Tab icon="code" title="HTML Snippet">

    #### 1. Install the Formo SDK

    Install this script in the `<head>` tag of your website. Replace `<YOUR_WRITE_KEY>` with the SDK Write key found in your project settings:

    ```tsx
    <script
      src="https://cdn.formo.so/analytics@latest"
      defer
      onload="
        window.formofy('<YOUR_WRITE_KEY>', {
          ready: function(formo) {
            formo.identify();
          }
        });
      "
    ></script>
    ```

    Calling [`identify`](/data/events/identify) at the start of every session or page load links wallets to user sessions.

    To improve security, enable [Subresource Integrity (SRI)](/security/sri).

    #### 2. Track events (optional)

    Formo autocaptures events like page views, wallet connects, signatures, and transactions for you.

    For everything else, use the [`track`](/data/events/track) function to track custom user actions specific to your app.

    ```html
    <button type="button" onclick="window.formo.track('Swap Completed', { foo: 'bar' })">
      Track event
    </button>
    ```

  </Tab>
  <Tab icon="react" title="React">

    <Note>
    **Using Wagmi?** Install with [Wagmi](#wagmi) instead for better event tracking.
    </Note>

    #### 1. Install the Formo SDK

    ```bash
      npm install @formo/analytics --save
    ```

    #### 2. Use `FormoAnalyticsProvider` in your app

    Wrap your React app in the provider provided by the SDK.

    ```tsx
      // App.tsx (or App.js)

      import { FormoAnalyticsProvider } from '@formo/analytics';
      import App from './App';

      const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);

      root.render(
        <React.StrictMode>
          <FormoAnalyticsProvider writeKey="<YOUR_WRITE_KEY>">
            <App />
          </FormoAnalyticsProvider>
        </React.StrictMode>
      );
    ```

    Replace `<YOUR_WRITE_KEY>` with the SDK Write key found in your project settings.

    #### 3. Identify users

    Call [`identify`](/data/events/identify) at the start of every session or page load to link a wallet address to a session.

    ```tsx

    import { useFormo } from "@formo/analytics";
    import { useAccount } from "wagmi";

    const HomePage = () => {
      const { address } = useAccount();
      const analytics = useFormo();

      useEffect(() => {
        if (address && analytics) {
          analytics.identify({ address });
        }
      }, [address, analytics]);
    }
    ```

    #### 4. Track events (optional)

    Formo autocaptures events like page views, wallet connects, signatures, and transactions for you.

    For everything else, use the [`track`](/data/events/track) function to track custom user actions specific to your app.

    ```tsx
      import { useFormo } from '@formo/analytics';

      const HomePage = () => {
        const analytics = useFormo();

        useEffect(() => {
          // Track a custom event
          analytics.track('Swap Completed', { points: 100 });
        }, [analytics]);

        return <div>Welcome to the Home Page!</div>;
      };

      export default HomePage;
    ```

  </Tab>
  <Tab icon="rectangle-terminal" title="Next.js">

    <Note>
    **Using Wagmi?** Install with [Wagmi](#wagmi) instead for better event tracking.
    </Note>

    #### 1. Install the Formo SDK

    ```bash
      npm install @formo/analytics --save
    ```

    #### 2. Use `FormoAnalyticsProvider` in your app

    Create a new `AnalyticsProvider.tsx` client component.

    <AccordionGroup>
      <Accordion title="App Router">
        ```tsx
          // AnalyticsProvider.tsx

          'use client';

          import { FormoAnalyticsProvider } from '@formo/analytics';

          type FormoAnalyticsProviderProps = {
            writeKey: string,
            children: React.ReactNode,
          };

          // The provider component
          export const AnalyticsProvider: FC<FormoAnalyticsProviderProps> = ({
            writeKey,
            children,
          }) => {
            return (
              <FormoAnalyticsProvider writeKey={writeKey}>
                {children}
              </FormoAnalyticsProvider>
            );
          };

          export default AnalyticsProvider;
        ```

        Wrap your Next.js app in your main layout file with the newly created `AnalyticsProvider` component:

        ```tsx
          // app/layout.tsx

          import { AnalyticsProvider } from './AnalyticsProvider';

          export default function RootLayout({
            children,
          }: {
            children: React.ReactNode,
          }) {
            return (
              <html lang='en'>
                <body>
                  <AnalyticsProvider writeKey='YOUR_WRITE_KEY'>
                    Your Page Content
                  </AnalyticsProvider>
                </body>
              </html>
            );
          }
        ```
      </Accordion>
      <Accordion title="Pages Router">
        ```tsx
          // AnalyticsProvider.tsx

          import { FormoAnalyticsProvider } from "@formo/analytics";

          type FormoAnalyticsProviderProps = {
            writeKey: string;
            children: React.ReactNode;
          };

          // The provider component
          export const AnalyticsProvider: FC<FormoAnalyticsProviderProps> = ({
            writeKey,
            children,
          }) => {
            return (
              <FormoAnalyticsProvider writeKey={writeKey}>
                {children}
              </FormoAnalyticsProvider>
            );
          };

          export default AnalyticsProvider;
        ```

        Wrap your Next.js app in your main layout file with the newly created `AnalyticsProvider` component:

        ```tsx
          // pages/_app.tsx

          import AnalyticsProvider from "@/AnalyticsProvider";
          import type { AppProps } from "next/app";

          export default function App({ Component, pageProps }: AppProps) {
            return (
              <AnalyticsProvider writeKey='YOUR_WRITE_KEY'>
                <Component {...pageProps} />
              </AnalyticsProvider>
            );
          }
        ```
      </Accordion>
    </AccordionGroup>

    Replace `<YOUR_WRITE_KEY>` with the SDK Write key found in your project settings.

    #### 3. Identify users

    Call [`identify`](/data/events/identify) at the start of every session or page load to link a wallet address to a session.

    ```tsx
    import { useFormo } from "@formo/analytics";
    import { useAccount } from "wagmi";

    const HomePage = () => {
      const { address } = useAccount();
      const analytics = useFormo();

      useEffect(() => {
        if (address && analytics) {
          analytics.identify({ address });
        }
      }, [address, analytics]);
    }
    ```

    #### 4. Track events (optional)

    Formo autocaptures events like page views, wallet connects, signatures, and transactions for you.

    For everything else, use the [`track`](/data/events/track) function to track custom user actions specific to your app.

    ```tsx
      import { useFormo } from '@formo/analytics';

      const HomePage = () => {
        const analytics = useFormo();

        useEffect(() => {
          // Track a custom event
          analytics.track('Swap Completed', { points: 100 });
        }, [analytics]);

        return <div>Welcome to the Home Page!</div>;
      };

      export default HomePage;
    ```

  </Tab>
</Tabs>

## Code Examples

<CardGroup>
  <Card title="Example Apps" icon="globe" iconType="solid" href="/sdks/web#code-examples">
    Working examples for Privy, Dynamic, React, and Next.js
  </Card>
</CardGroup>

## Autocapture

The Formo SDK automatically captures common events such as page views and wallet events (connect, disconnect, signature, transaction, etc) with full attribution (referrer, UTM, referrals.)

You do not need to configure anything to track these events.

## Verification

To verify that the SDK is installed correctly, navigate to your site and open the network tab of the developer tools in your browser.

Go to your browser's Network tab and and look for successful 'raw_events' request in the network console. Check that the request returns a 202 response status. (Note that it may take up to a minute due to the flush interval.)

Events that are tracked correctly should also show up in the Activity page of your Formo workspace.

## SDK Reference

<CardGroup>
  <Card title="Web SDK" icon="globe" iconType="solid" href="/sdks/web">
    Web SDK (HTML, React, Next.js)
  </Card>
</CardGroup>

## Support

<CardGroup cols={2}>
  <Card title="Slack" icon="slack" href="https://formo.so/slack">
    Join our Slack community
  </Card>
  <Card title="Twitter" icon="x" href="https://x.com/getformo">
    Send us a tweet or DM us any time
  </Card>
  <Card title="Email" icon="envelope" href="mailto:support@formo.so">
    Email us at [support@formo.so](mailto:support@formo.so)
  </Card>
  <Card title="Founder" icon="comments" href="mailto:yos@formo.so">
    Bug him at [yos@formo.so](mailto:yos@formo.so) (share anything that's on
    your mind - the good and the bad)
  </Card>
</CardGroup>
